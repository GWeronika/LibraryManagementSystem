package com.library.librarysys.users;

//guest support will be here soon
public class User {
    public String createAccount() {
        //the account will only be created if the email address is not repeated in the database (the database does not accept duplicates)
        return "No implementation";
    }
    public String searchForLibraryBranch() {
        //displaying all rows of the Library table
        return "No implementation";
    }
    public String browseResources() {
        //option to search collections by title or author - possible modifications in the future (search by keywords in the blurb)
        return "No implementation";
    }
}
